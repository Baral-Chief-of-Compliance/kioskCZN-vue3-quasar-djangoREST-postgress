# Django app API Киоск Центр занятости МО

Это API киосков центра занятости МО

## 🛠️ Подготовка (PROD)

Данный сервис запускается на сервере, который не имеет доступ к репозиториям docker образов, а также на нем имееются проблемы с получением пакетов для python

linux
```bash
docker build -t kiosk-czn-django-api:v2.0 -f ./Dockerfile.for-build .
```
Windows
```shell
docker build -t kiosk-czn-django-api:v2.0 -f .\Dockerfile.for-build .
```


После успешной сборки, необходимо сделать архив нашего образа

Windows/Linux данную команду прописываем в директории (на флешке), так как именно там появиться архив
```bash
docker save -o kiosk_czn_django_api_v2.0.tar kiosk-czn-django-api:v2.0
```

Переходим на машину в закрытом контуре, перекидываем туда наш проект и архив с образом. Далее в директории с архивом прописываем команду

```bash
docker load -i kiosk_czn_django_api_v2.0.tar 
```

